# 发布检查记录（2026）

- 沿用用户已确认可运行的版本。发布仅调整标题、说明、静态构建与站点资源路径；不继续模型修复。
- 发布模型与确认版本逐字节一致，SHA-256：`2ebfd3f314b9e95ab78cdf98bfbc2a58025af8bdf2f73073ba4ed37444852f57`。
- 漫游 GLB 内嵌一张 4096 × 4096 贴图。仓库不含原始扫描模型、OBJ、USDZ、原始照片、深度图或压缩备份。
- 发布目录从必要文件建立，未沿用旧 Git 历史。排除缓存、个人回忆、本地工作脚本、聊天附件和环境配置。
- 对发布文本检查本机绝对路径、常见密钥格式、手机号及私钥内容，未发现匹配。
- 对运行贴图进行重叠分块、四种方向 OCR 及二维码检测，并人工检查整张贴图与识别区域；未发现可识别的私人姓名、手机号、聊天记录或二维码。OCR 的少量结果为物品字样、数字或误识别，因此未改动贴图。
- 自动识别和人工检查不能保证识别所有低清晰度或被切分的内容；这里记录已执行的检查范围，不宣称绝对无遗漏。
- 发布构建通过 TypeScript 检查。已在与 GitHub Pages 相同的子路径检查模型加载、入口、俯视、昼夜按钮与回忆卡片。移动、碰撞和触屏摇杆沿用已验证逻辑。
- 回忆内容只保存于访问者当前浏览器，没有预置个人经历，也不会上传服务器。

## 发布内容

`app/`、`components/`、`lib/` 为运行源码；`public/` 为必要静态资源；`docs/` 为 GitHub Pages 发布目录。模型在源码资源与发布目录中各有一份，二者内容相同。

## 未解决的扫描局限

沿用版本的细杆、门窗、遮挡区域和贴图中仍存在扫描缺损及日光痕迹。本次发布不进行额外重建或美化。
